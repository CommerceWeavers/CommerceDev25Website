---
import { Image } from 'astro:assets';
import cdLogo from '../assets/commerce-dev-logo-hor.svg';

const currentLang = Astro.url.pathname.split('/')[1] == 'en' ? 'en' : 'pl';
let str;
if (currentLang === 'en') {
    str = await import('../content/en/strings.md');
} else {
    str = await import('../content/pl/strings.md');
}
---

<div class="container mx-auto mt-16 mb-14 px-8 md:grid grid-cols-10 gap-4">
    <div class="hidden md:block col-span-6 border-b-2"></div>
    <div class="col-span-4">
        <a href="/" class="stat">
            <Image src={cdLogo} alt="commerce_dev" style="width: 100%;" />
        </a>
    </div>
</div>
